import { DataComment } from "store/post/reducer"

type CommentProps = {
    comment: DataComment;
}

const Comment = (props: CommentProps) => {
    const { comment } = props;
    return (
        <div>
            
        </div>
    )
}

